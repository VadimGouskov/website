import * as React from "react";

interface Props {}

const Skillset: React.FC<Props> = (props) => {
    return (
        <>
            <h1>Skillset</h1>
        </>
    );
};

export default Skillset;
